<% breadcrumbs_for @territory_decorator %>

<%= render 'form', territory: @territory_decorator.form_object, territory_decorator: @territory_decorator %>

<%= button_group(
  @territory_decorator.show_button,
  @territory_decorator.cancel_button
) %>
