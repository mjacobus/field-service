<%= simple_form_for(publisher) do |f| %>
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :phone %>
    <%= f.input :congregation_member, as: :radio_buttons, boolean_style: :inline %>
    <%= publisher_decorator.save_button %>
<% end %>
